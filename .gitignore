# Python build scripts (not needed for the deployed site)
__pycache__/
*.pyc

# Large source data files not needed for deployment are committed
# since GitHub Pages serves them as static assets

# Local IDE/agent config and debugging artifacts
.claude/
test_flag.png

# Pipeline / local server logs
run_all.log
server.log
extract_data.log

# Local debugging artifacts
debug_*.png
